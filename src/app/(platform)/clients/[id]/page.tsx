import { notFound } from "next/navigation"; import { getCurrentContext } from "@/lib/auth/context";
export default async function Client({params}:{params:Promise<{id:string}>}){const {id}=await params;const c=await getCurrentContext();if(id!=="vyo-studios"||(!c!.permissions.includes("clients.manage")&&!c!.clientIds.includes(id)))notFound();return <div className="mx-auto max-w-6xl px-5 py-10 sm:px-8"><p className="text-xs tracking-[.16em] text-[#8d8f88]">CLIENT WORKSPACE</p><h1 className="mt-3 text-3xl tracking-tight">VYŌ Studios</h1><p className="mt-3 text-[#a8a9a2]">Shared context for agent work, campaigns, documents and activity.</p><div className="mt-10 grid gap-px border border-[#2b2d29] bg-[#2b2d29] md:grid-cols-3">{["Company context","Active agents","Activity"].map(x=><div className="min-h-32 bg-[#171815] p-5" key={x}><p className="text-xs tracking-[.14em] text-[#8d8f88]">{x.toUpperCase()}</p><p className="mt-5 text-[#a8a9a2]">Ready for connected data</p></div>)}</div></div>}
