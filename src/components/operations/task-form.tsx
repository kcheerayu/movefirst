type Choice = { id: string; name: string };
type Project = { id: string; name: string; client_id: string };
type Task = { id?: string; title?: string; description?: string | null; client_id?: string; project_id?: string | null; assignee_id?: string | null; priority?: string; status?: string; due_date?: string | null };
export function TaskForm({ action, clients, projects, users, task = {}, fixedClientId }: { action: (formData: FormData) => void | Promise<void>; clients: Choice[]; projects: Project[]; users: Choice[]; task?: Task; fixedClientId?: string }) {
  const selectedClient = fixedClientId ?? task.client_id ?? "";
  return <form action={action} className="grid gap-3 border border-[#2b2d29] bg-[#171815] p-5 sm:grid-cols-2"><input type="hidden" name="id" value={task.id ?? ""}/>{fixedClientId ? <input type="hidden" name="clientId" value={fixedClientId}/> : <label className="text-xs text-[#b5b6ae]">Client<select required name="clientId" defaultValue={selectedClient} className="mt-2 w-full border border-[#343630] bg-[#10110f] p-2.5"><option value="">Choose client</option>{clients.map((client) => <option value={client.id} key={client.id}>{client.name}</option>)}</select></label>}<label className="text-xs text-[#b5b6ae]">Task title<input required name="title" defaultValue={task.title ?? ""} className="mt-2 w-full border border-[#343630] bg-[#10110f] p-2.5"/></label><label className="text-xs text-[#b5b6ae]">Project<select name="projectId" defaultValue={task.project_id ?? ""} className="mt-2 w-full border border-[#343630] bg-[#10110f] p-2.5"><option value="">No project</option>{projects.filter((project) => !selectedClient || project.client_id === selectedClient).map((project) => <option value={project.id} key={project.id}>{project.name}</option>)}</select></label><label className="text-xs text-[#b5b6ae]">Assignee<select name="assigneeId" defaultValue={task.assignee_id ?? ""} className="mt-2 w-full border border-[#343630] bg-[#10110f] p-2.5"><option value="">Unassigned</option>{users.map((user) => <option value={user.id} key={user.id}>{user.name}</option>)}</select></label><label className="text-xs text-[#b5b6ae]">Priority<select name="priority" defaultValue={task.priority ?? "MEDIUM"} className="mt-2 w-full border border-[#343630] bg-[#10110f] p-2.5"><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>URGENT</option></select></label><label className="text-xs text-[#b5b6ae]">Status<select name="status" defaultValue={task.status ?? "TODO"} className="mt-2 w-full border border-[#343630] bg-[#10110f] p-2.5"><option>TODO</option><option>IN_PROGRESS</option><option>SUBMITTED</option><option>BLOCKED</option><option>DONE</option></select></label><label className="text-xs text-[#b5b6ae]">Due date<input name="dueDate" type="date" defaultValue={task.due_date ?? ""} className="mt-2 w-full border border-[#343630] bg-[#10110f] p-2.5"/></label><label className="text-xs text-[#b5b6ae] sm:col-span-2">Description<textarea name="description" defaultValue={task.description ?? ""} className="mt-2 min-h-20 w-full border border-[#343630] bg-[#10110f] p-2.5"/></label><button className="justify-self-start bg-[#c8f54b] px-4 py-2 text-sm font-medium text-[#10110f]">{task.id ? "Save task" : "Create task"}</button></form>;
}
